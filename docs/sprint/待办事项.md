# 待办事项

> 已确认暂不处理、或有价值但未排期的长期任务。

## 暂不做（边界声明）

- 不允许并行 Agent 同时写文件——同一时刻只有一个 Agent 持有写权限
- 不引入真实 LLM/API 调用测试——保持测试纯确定性
- 不把 researcher/子 Agent 原始全文无节制注入主 Agent 上下文——始终走摘要+截断
- 不把"前端多 Agent 时间线"提前到工作流引擎之前——先有数据再有展示

## 长期积压（v0.6+）

- `Checkpoint` / `FileSnapshot` 类型已定义，未形成完整回滚历史系统
- 工作流模板市场：用户可分享和导入预定义工作流（如"代码审查"、"技术调研"、"重构"）
- 多项目并行会话：同时打开多个项目，每个有独立会话和工作流
- Agent 间直接通信协议：不通过 orchestrator 中转，Agent 可直接向其他 Agent 发送结构化消息
- 工作流断点续跑：会话关闭后重开，从上次中断的阶段继续
- 人机协同工作流：在特定阶段（如 plan 产出后）暂停等待用户审核，用户可修改计划后继续

## 已完成（归档）

> 以下能力已在 v0.2 和 v0.3 中落地，记录在此供回溯。

### v0.2 完成项

- Web UI 工作台：项目打开、最近项目、聊天流、工具调用卡片、设置面板、外观配置
- 单 Agent Tool Calling：LLM streaming、tool calls、多轮工具循环
- 本地代码工具：读文件、写文件、编辑、grep、find、ls、console
- FileStagingArea：文件写入记录 baseline，成功后生成 diff，异常时 rollback
- 命令审批：非 YOLO 模式下 `run_console` 执行前通过 WebSocket 请求审批
- 三模式开关：`yolo_mode`、`auto_review`、`solo_mode`
- Agent CRUD：创建、编辑、删除 Agent 定义，配置模型、工具和提示词
- Orchestrator 编排：`AgentOrchestrator.run_user_message()` 接管用户消息执行路径
- 只读委派：`delegate_agent` 对 researcher 走只读路径
- 并行 researcher：非 Solo 模式受控触发，`Semaphore` 限并发，超时隔离
- Research summary 注入：合并摘要以 `[Parallel Research Summary]` 注入 main Agent 上下文，12k 字符上限
- 安全 handoff：`delegate_agent` 对 coder 等非 researcher 走串行 handoff，复用主流程 staging/permission
- Handoff 事件展示：前端展示 `handoff.started` / `handoff.completed` / `handoff.failed`
- LLM 语义标题：异步 LLM 生成会话标题，支持独立 `title_model` 配置，算法 fallback
- 单窗口启动：`start.bat` 改为前台单窗口运行
- 自动化测试：125 个测试通过
- 安全分层：命令风险分级（只读/普通/高危）、只读白名单自动放行、高危命令强制审批、路径边界保护

### v0.3 完成项

- **per-agent 工具权限策略**：从二值判断升级为 per-agent 细粒度权限；路径约束（`allowed_paths` / `denied_paths`）；命令风险预算（`max_command_risk`）；`AgentDefinition.permissions`（`AgentPermissions` dataclass）；27 个新测试
- **内置角色 Agent 定义**：planner / coder / reviewer 三内置角色 + main；默认 `agents.json` 4 个 Agent；`role` 语义化：`build_system_prompt()` 角色专属提示词
- **基础模块测试补全**：SessionStore (14) + FileStagingArea (19) + EditTool (13) + PermissionManager (19)；总计 217 测试全部通过
- **Setup Wizard**：三步引导（服务商 → API Key → 角色一览）；首次运行检测（`setup_completed`）
- **GUI 优化**：设置分区 Tab（模型 / Agent / 外观 / 安全）；Agent 权限编辑 UI（路径约束、风险预算、委派/Handoff 开关）；安全设置页（YOLO / 自动审查 / Solo 全局开关）
- **代码质量改进**：APIProvider.to_dict 明文 key 泄露修复；QUICK_PRESETS / CLIENT_TYPE_OPTIONS 提取到 constants.ts；Tool 基类 `_resolve_and_check_path()` 消除 6 工具 × 8 行重复；AgentPermissions/AgentDefinition.to_dict 统一 asdict()；ws_server.py PUT /api/agents permissions setattr Bug 修复；前端 utils/api.ts + useAsyncAction.ts 消除 21 处 fetch/setBusy 重复；edit.py newline='' 修复消除 Linux CI CRLF 失败（+ 回归守卫测试）；约定文档补充新约定（newline / api.ts / useAsyncAction / _resolve_and_check_path）；四轮审查：0 死导入，0 lint error，净减 201 行

### v0.4 完成项（Step 1-7 + 打磨）

- **工作流数据类型**（Step 1）：`TaskList` / `DiffSet` / `ReviewReport` / `WorkflowState` + 序列化往返测试
- **WorkflowRunner 状态机**（Step 2）：Plan → Code → Review 阶段流转骨架 + Mock Agent 测试
- **阶段感知提示词**（Step 3）：`build_phase_prompt()` 四阶段上下文注入（PLANNING / CODING / REVIEWING / FEEDBACK）
- **产出物解析**（Step 4）：`parse_task_list()` / `parse_review_report()` 文本标记 + 启发式回退
- **自动触发串联**（Step 5）：plan → code → review 完整链路自动流转
- **用户命令处理**（Step 6）：approve_plan / reject_plan / start_review / skip_review / retry / skip_task / undo / abort / resume（32 测试）
- **阶段状态持久化**（Step 7）：`WorkflowState` 序列化 + `--resume` 恢复 + `_save_session` 自动持久化（33 测试）
- **项目上下文增强**：`repo_map.py` 深度扫描项目结构（4 层、AST 签名提取、token 预算）；阶段间上下文传递（planner 概述 → coder/reviewer、已完成任务清单 → 后续阶段）
- **CLI 彩色输出系统**：`cli_display.py` — ANSI 颜色码 + Windows VT100 + TTY 降级；角色/阶段配色；进度条、横幅、审查结果格式化
- **Spinner 动画**：agent 思考时旋转动画（后台线程、0.08s/帧、非 TTY 静默）
- **实时 Diff 展示**：orchestrator 广播 `files.changed` 事件；CLI 彩色渲染 unified diff（增行绿/删行红/文件头青/hunk 头洋红、50 行截断、文件变更摘要）
- **`/undo` 撤销命令**：CODE_REVIEW / FEEDBACK 阶段回退上一个已完成任务，重置状态
- **引擎健壮性**：FEEDBACK 循环重试上限（`MAX_RETRIES_PER_TASK=3`，超限自动跳过）；任务索引感知（"任务 1/5" 格式）；`total_files_changed` 累计统计；`retry_count` 持久化
- **CLI 增强功能**：`--verbose/-v` 调试日志、`--version`、用法示例、Ctrl+C 优雅退出（exit 130）；会话列表排序 + 编号；工具调用序号；阶段耗时统计；完成摘要（文件变更数/跳过任务数/逐任务状态 ✓/-/○）
- **测试覆盖**：517 个测试全部通过（含上下文集成 33 + CLI 显示 60 + 打磨 17 + 新增 34）

### v0.4 完成项（Step 8-9）

- **Step 8 前端工作流时间线**：后端 5 个结构化工作流事件类型（plan_shown/task_started/task_completed/review_result/completed）+ engine 广播 + ws_server 命令处理；前端 WorkflowTimeline 组件（计划卡片/任务进度/审查结果/完成统计）；MessageInput 工作流模式切换按钮；Timeline 组件工作流事件图标/标签；SessionContext workflow state 管理；修复 onAgentStatus idle 阶段判断（completed/plan_review/code_review/feedback 正确解除处理状态）；558 测试通过 + 前端构建成功
- **Step 9 端到端联调**：41 个边界 case 测试（全部跳过路径/大量文件变更/半畸形解析/大数据持久化往返）；Planner/Reviewer 提示词修复（结构化 JSON 模板）；files.changed 广播去重；ReviewReport 解析器关键词优先级修复；真实 LLM 全链路验证 PASS（4/4 任务完成，58 次工具调用）；Session 持久化恢复 PASS；/undo 状态回退 PASS；558 测试全部通过